1. Selected the headers with the help of shift (Ctrl + shift along with the arrow will also do) and filled them with a different color so that they can be distinguished from the rest of the data points.
2. Then I made sure that the column headers are in PROPER format. I did that first by injecting a helper row just below it. Then typed the <=PROPER(A1)> formula for the 1st cell. Post that to implement the same formula for the rest of the column headers I could have used the fill handle and drag it along the rows. However, it can be a cumbersome approach given how there can be a large number of rows. Hence I selected the row using <shift> 
